{% extends 'books/base.html.twig' %}
{% block body %}<div class="container-field">


    <div class="container">
        <div class="row">
            <div class="col 12">
                {{ form_start(form) }}
                {{ form_row(form.title)}}
                {{ form_row(form.author) }}
                {{ form_row(form.year, {'date_pattern': '<span style="display: none;">{{ day }}{{ month }}</span>{{ year }}'}) }}
                {{ form_row(form.save, {'label': 'Сохранить'}) }}
                {{ form_end(form) }}
            </div>
        </div>
    </div>

    {% endblock %}